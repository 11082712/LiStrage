<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.hal.nagoya.nhs70244.list">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".detailsActivity" />
        <activity android:name=".CosActivity" />
        <activity android:name=".CosSereActivity" />
        <activity android:name=".CosTagActivity" />
        <activity android:name=".SerchActivity" />
        <activity android:name=".StrageActivity"></activity>
    </application>

</manifest>